package ch4;

public class ChkNum {
    //вернуть логическое значение true
    //если x содердит четное число
    boolean isEven(int x) {
        if((x % 2) == 0) return true;
        else return false;
    }
}
